spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/notifications_test
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  data:
    redis:
      host: localhost
      port: 6379

app:
  jwt:
    secret: test-secret-must-be-at-least-256-bits-long-for-hs256
    access-token-expiry: 900000
    refresh-token-expiry: 604800000
  encryption:
    key: ""

gmail:
  oauth2:
    client-id: test-client-id
    client-secret: test-client-secret
    redirect-uri: http://localhost:8080/api/v1/email-accounts/callback
    scopes: https://www.googleapis.com/auth/gmail.readonly
    auth-uri: https://accounts.google.com/o/oauth2/v2/auth
    token-uri: https://oauth2.googleapis.com/token
  scan:
    interval: 60000

slack:
  bot-token: xoxb-test-token

twilio:
  account-sid: ""
  auth-token: ""
  whatsapp:
    from-number: whatsapp:+14155238886

logging:
  level:
    com.notifications: DEBUG
